# Mingmai
## Install
```bash
# Build
cargo build --release

# Install
install -m 755 target/release/mingmai "$HOME/.local/bin/mingmai"

# Verify
which mingmai
```

## Inspector
```bash
# Run:
npx @modelcontextprotocol/inspector

# UI:
http://localhost:6274
```
